---
-   name: set hostname
    gather_facts: false
    hosts:
    - ubuntu
    tasks:
    -   name: set hostname to ubuntu-ai-{{host_id}}
        ansible.builtin.hostname:
            name: ubuntu-ai-{{host_id}}
            use: debian